# List = Finding Items 
# Find the index of a given item in a list
letters = ["a", "b", "c"]

print(letters.index("a"))

# If the item is not in the list, we will get an error
# use a conditinal to prevent the error, if item is in the list print index

if "d" in letters:
  print(letters.index("d")

# count() returns the number of occurences of a given item in a list
letters.count("d")

